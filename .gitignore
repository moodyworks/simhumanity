.env
.venv/
__pycache__/
*.pyc
*.sqlite3
*.db
/var/
.DS_Store
# Runtime debug placements from the in-game place tool.
place_overrides.json
world_place_overrides.json
# Large world-map source + generated chunks (regenerate with tools/tile_world.py)
world_src.jpg
world.2004*.jpg
world_tiles/
# Regenerable build/verification artifacts (run tools/build_map.py /
# tools/preview_landmarks.py). The baked grid server/med_map.txt IS committed;
# these preview images are not.
med_classified_preview.png
med_landmarks_preview.png
med_sites_final.png
# NASA source imagery (500m tiles + 8km overview) — large, public domain, regenerable assets
highres/
client/minimap.jpg
# Obsolete Dymaxion source tiles (abandoned for Blue Marble)
[1-9]-[0-9].jpg
# User screenshots
Screenshot*.jpg
*.screenshot.*
